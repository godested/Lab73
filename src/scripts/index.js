import $ from 'jquery';
import { TimelineLite, Power1 } from 'gsap';

$(document).ready(function () {
  const timeLine = new TimelineLite();

  const logo = $('.logo');

 // timeLine.fromTo(logo, 1, {opacity: 0, y: -100}, {opacity: 1, y: 0, ease: Power1.easeIn})
});
